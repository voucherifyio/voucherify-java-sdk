

# ValidationRuleErrorLibrary

References an Error Message Library entry. Required when `mode` is `LIBRARY`. Must be omitted or `null` when `mode` is `MESSAGES`.

## Properties

| Name | Type | Description |
|------------ | ------------- | ------------- |
|**key** | **String** | Identifies the library message. Use a validation-rule name such as &#x60;order.amount&#x60;, or a custom attribute key such as &#x60;order.metadata.location&#x60;. |



